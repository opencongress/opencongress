<p>
  Bill Sponsor: <a href="<%= url_for(:only_path => false, :controller => 'people', :action => 'show', :id => @bill.sponsor) %>"><%= @bill.sponsor.name %></a><br />
  <% unless @bill.related_bills.empty? %>
    Related Bills: <%= @bill.related_bills.collect { |rb| "<a href='#{url_for(:only_path => false, :controller => 'bill', :action => 'show', :id => rb.ident)}'>#{rb.title_typenumber_only}</a>"}.join(", ") %><br />
  <% end %>
</p>
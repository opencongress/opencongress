Thanks! Your vote has been recorded in your OpenCongress profile. Now you might want to <a href="<%= url_for(:controller => 'bill', :action => 'show', :id => @bill.ident, :anchor => 'comments') %>">add a comment</a> about why you <% if @bv.support == 0 %>support<% else %>oppose<% end %> this bill, and rate the helpfulness of other comments. <%= link_to_function('(Close)', "Element.hide('vote_message_#{@bill.id}');") %>

- subtitle ||= nil
#heading.group
  
  .button.silver.large.floatright.margin-top.margin-right
    Join this Group

  .button.silver.large.floatright.margin-top.margin-right
    Invite People to this Group

  .thumb
    = image_tag @group.group_image.url(:thumb)

  %h2= subtitle.nil? ? @group.name : "#{@group.name} : #{subtitle}"
  
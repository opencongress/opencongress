.left_menu
  - active_class = action_name =~ /.*index.*/ ? 'active' : ''
  .link.header{:class =>active_class }
    = link_to "Home", { :controller=>'welcome'}

//  - active_class = action_name =~ /.*how_its_work.*/ ? 'active' : ''
//  .link.header{:class =>active_class }
//    = link_to "How it's works", { :controller => :welcome, :action => :how_its_work}
//  .elements
//    .link
//      = link_to "sublink", { :controller=>'', :action => '' }
